topgenerator
============

Run:

$ python setup.py install
$ python setup.py develop

Start Python:

>>> import pyorient
>>> import topgenerator
>>> from topgenerator.gen import *

Create a random network:

>>> create_newtwork()

Delete all nodes:

>>> clear_network()
